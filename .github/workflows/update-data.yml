name: 每日更新股票数据

# 触发条件：
# 1. 每个工作日北京时间15:30(收盘后)自动运行一次
# 2. 也支持在GitHub网页上手动点击运行，方便你测试
on:
  schedule:
    - cron: '30 7 * * 1-5'   # UTC时间7:30 = 北京时间15:30，周一到周五
  workflow_dispatch: {}

# 让这个自动化任务有权限把新数据提交回仓库
permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: 拉取仓库代码
        uses: actions/checkout@v4

      - name: 配置Python环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 安装依赖库
        run: pip install -r requirements.txt

      - name: 抓取历史行情数据
        run: python fetch_history.py

      - name: 抓取板块数据(只在周一跑，板块归属不常变，节省资源)
        continue-on-error: true   # 板块数据源如果临时访问不了，不影响其他数据正常更新
        run: |
          if [ "$(date -u +%u)" = "1" ] || [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            python fetch_sector.py
          else
            echo "今天不是周一，跳过板块数据抓取(用之前已经抓好的数据)"
          fi

      - name: 计算技术指标并生成报表
        run: python analyze_history.py

      - name: 把最新数据提交回仓库
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add history_data sector_data 选股数据.json 分析报表.xlsx
          git diff --cached --quiet && echo "数据没有变化，跳过提交" || git commit -m "自动更新股票数据 $(date +'%Y-%m-%d %H:%M')"
          git push
